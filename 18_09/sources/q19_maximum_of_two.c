// WAP to find maximum of two numbers by using if-else statement
#include <stdio.h>
int main() {
	int a,b; // assuming a > b therefore,
	a = 10;
	b = 5;
	printf("By Tausif Alam, BCA-1EA\n\n");
	if (a > b) {
		printf("%d is greated than %d", a,b);
	} else {
		printf("%d is greated than %d", b,a);
	}
	return 0;
}
