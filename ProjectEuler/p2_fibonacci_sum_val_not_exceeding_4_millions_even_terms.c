#include <stdio.h>

// specialised fibonacci sequence finder function
int fibonacciNum(int range);
// summation handler function to perform additions on large-ranged data
long long int sumOfLargeSet(long long int numArray[]);
int main() {
    // get the range

}